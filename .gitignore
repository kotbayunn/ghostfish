# Активная тема Ghostty — создаётся install.sh / функцией theme,
# у каждого своя, в репозитории не версионируется.
config/ghostty/themes/current.conf

# Бэкапы, которые делает install.sh при первом запуске.
*.bak.*

# Локальные заметки/ТЗ — не публикуем.
tz.txt

# Файл универсальных переменных fish (если вдруг окажется рядом).
fish_variables

# Системный мусор.
.DS_Store
Thumbs.db

# Артефакты codex-review (служебное, не публикуем).
.codex-review/

# Локальные артефакты Claude Code.
.claude/
